server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # All routes fall back to index.html so React Router handles them.
    # Without this, a direct visit to /dashboard would return a 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
